{
    "build": {
        "dockerfile": "Dockerfile",
        "context": ".."
    },
    "runArgs": [
        "--gpus", "all"
    ],
    "postCreateCommand": "apt-get update && apt-get install -y git && rm -rf /var/lib/apt/lists/*"
}